# 6-3 학급 링크 모음 — 작업 지침

초등학교 6학년 3반 태블릿 수업용 링크 모음 페이지 (GitHub Pages로 서비스).
사용자는 교사이며 개발자가 아니므로, 결과를 쉬운 한국어로 설명한다.

## 링크/공지 수정 워크플로 (가장 흔한 요청)

교사가 "링크 추가해줘", "○○ 빼줘", "공지 올려줘", "오늘의 링크로 지정해줘" 등을 요청하면:

1. **`links.json`만 수정한다** (`index.html`은 디자인 변경 요청이 아니면 건드리지 않음)
   - `updated` 필드를 오늘 날짜로 갱신
   - 새 링크에는 어울리는 `emoji`를 넣고 `isNew: true`로 NEW 배지 표시
   - `featured: true`는 "오늘의 링크"로 맨 위에 크게 표시됨 (요청 시에만)
   - `notice`에 문구가 있으면 노란 공지 배너로 표시됨
2. 커밋 후 지정된 작업 브랜치에 푸시
3. PR을 만들고 **바로 머지까지 진행한다** — 사용자가 링크/공지 수정 건에 대해서는
   "요청 한 번에 PR 생성부터 머지(반영)까지" 자동으로 처리하는 것을 상시 허용했음.
   머지 후 "1~2분 뒤 학생 화면에 반영된다"고 알려줄 것.

디자인·코드 구조 변경 등 링크/공지 이외의 변경은 PR까지만 만들고 머지 전에 사용자 확인을 받는다.

## 파일 구성

- `links.json` — 링크 목록·공지·업데이트 날짜 (데이터는 전부 여기)
- `index.html` — 디자인과 동작. links.json을 `no-store` + 타임스탬프로 매번 새로 fetch하고,
  페이지가 다시 보일 때마다 자동 갱신함 (학생 태블릿 캐시 문제 해결용 — 이 구조를 깨지 말 것)
- `sw.js` — 통합 서비스 워커 (네트워크 우선 캐싱 + OneSignal 푸시).
  `OneSignalSDKWorker.js`는 구버전 기기 호환용으로 sw.js를 불러오기만 한다 — 삭제 금지
- `manifest.json` — 홈 화면 추가(PWA) 설정

## 주의

- 폰트는 Noto Sans KR (사용자 지정)
- 학생들이 홈 화면 바로가기로 접속하므로 파일 경로/이름을 바꾸지 말 것
- links.json은 유효한 JSON이어야 함 — 수정 후 반드시 파싱 검증할 것
